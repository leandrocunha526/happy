version: 2.1

jobs:
  build-project:
    docker:
      - image: circleci/node:12.19.0

    working_directory: ~/happy/web

    steps:
      - checkout
      - run: npm install
      - run: npm run lint
      - run: npm run build
